spring:
  flyway:
    locations: classpath:/db-migration
    group: true
    ignoreMissingMigrations: true

security:
  jwtToken:
    secret:
      value: dummy
      algorithm: HmacSHA512
    expirationTimeInSeconds: 3600 # 5 minutes
