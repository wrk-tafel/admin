stages:
  - build
  - test
  - e2e-test
  - deploy-test
  - deploy-production

build-backend:
  stage: build
  script:
    - gradlew :backend:bootBuildImage

build-frontend:
  stage: build
  script:
    - gradlew :frontend:npm_run_build :frontend:docker

test-backend:
  stage: test
  script:
    - gradlew :backend:test

test-frontend:
  stage: test
  script:
    - gradlew :frontend:test

e2e-test:
  stage: e2e-test
  script:
    - echo "e2e-test"
  when: manual

deploy-to-test:
  stage: deploy-test
  environment:
    name: test
    url: "https://adminneu-test.tafel1030.at"
  script:
    - echo "deploy"
  when: manual

deploy-to-production:
  stage: deploy-production
  environment:
    name: prod
    url: "https://adminneu.tafel1030.at"
  script:
    - echo "deploy"
  when: manual
