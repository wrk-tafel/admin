This directory stores MCP server configurations for the repository.

Supported MCP servers:
- cypress: configuration for Cypress E2E runner

Files are JSON objects describing server name, type, project_root, commands and CI integration hints.
