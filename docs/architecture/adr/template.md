# ADR-NNNN: Short title in the imperative or as a noun phrase

**Status:** accepted · **Recorded:** YYYY-MM-DD

## Context

What forced a decision. The constraints that were actually in play — team size, operating
environment, existing code, deadlines — not a general description of the feature. A reader should be
able to tell from this section alone why the obvious answer was not obviously right.

## Decision

What was decided, stated plainly and in the present tense. Point at the code that implements it
(`path/to/File.kt`), so the ADR can be checked against reality.

## Consequences

What this costs and what it buys, both stated honestly. Include the parts that are annoying: the
rules contributors now have to remember, the things that got harder, the limits this puts on future
change.

## Alternatives considered

Each realistic alternative with the reason it lost. "We didn't think of it" is not a reason; if an
option was never seriously on the table, leave it out rather than inventing a rejection.

## References

Issues, PRs, module READMEs and source files that back up the above.
</content>
</invoke>
