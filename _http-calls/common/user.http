### Login
POST {{baseApiPath}}/login?username={{username}}&password={{password}}

> {%
    client.global.set("jwt", response.body["token"]);
%}

### Login (fails)
POST {{baseApiPath}}/login?username=wrong&password=wrong

> {%
    client.global.set("jwt", response.body["token"]);
%}

### Change password
POST {{baseApiPath}}/users/change-password
Authorization: Bearer {{jwt}}
Content-Type: application/json

{
  "passwordCurrent": "12345",
  "passwordNew": "wrk-tafel"
}
